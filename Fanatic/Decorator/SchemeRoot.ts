// The base archetype of the scheme; usually a singular object, but sometimes a collection of objects and we don't even handle that so pound sand chuck.

export function SchemeRoot() {
    return function(target: any, property: any) {
    };
}
