import { CollectionField } from "./CollectionField"

// A single field to be rendered in a form or as part of a header component, or inline on a table (list)
export class MultiFileField extends CollectionField {
}
