{
  "name": "heretic",
  "version": "1.0.0",
  "description": "",
  "main": "index.ts",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "cd ../ && node ./Script/spell.js && cd ./Heretic && tsc -b tsconfig.json --verbose",
    "clean": "tsc -b tsconfig.json --clean"
  },
  "files": [
    "../Sword/*.ts",
    "../Sword/**/*.ts",
    "../Fanatic/**/*.ts",
    "../Pulpit/**/*.ts",
    "../Witch/**/*.ts"
  ],
  "keywords": [],
  "author": "",
  "license": "ISC"
}
